import { describe, it, expect } from 'vitest';

describe('customer side', () => {
	it('products', () => {
		expect(1 + 2).toBe(3);
	});
	it('products variant', () => {
		expect(1 + 2).toBe(3);
	});
	it('add to cart', () => {
		expect(1 + 2).toBe(3);
	});
	it('show cart', () => {
		expect(1 + 2).toBe(3);
	});
	it('remove from cart', () => {
		expect(1 + 2).toBe(3);
	});
	it('check subtotal', () => {
		expect(1 + 2).toBe(3);
	});
	it('localstorage get', () => {
		expect(1 + 2).toBe(3);
	});
	it('localstorage set', () => {
		expect(1 + 2).toBe(3);
	});
	it('localstorage clear', () => {
		expect(1 + 2).toBe(3);
	});
});
describe('managment side', () => {
	it('add supply', () => {
		expect(1 + 2).toBe(3);
	});
	it('adjust supply quantity', () => {
		expect(1 + 2).toBe(3);
	});
	it('check threshold warning', () => {
		expect(1 + 2).toBe(3);
	});
	it('update supply', () => {
		expect(1 + 2).toBe(3);
	});
	it('remove supply', () => {
		expect(1 + 2).toBe(3);
	});
});

